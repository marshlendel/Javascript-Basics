//Ternaries are good for testing a value with one of two outcomes like an if else. But it uses up a lot less space, usually only one line

// (Condition) ? run this code : run this code instead

let count = 1;

(count > 1) ? console.log("I have more than one cookie!") : console.log("I have no cookies.")